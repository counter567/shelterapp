{
  "private": true,
  "scripts": {
    "build": "dotenv -- turbo run build",
    "prebuild": "npm run generate",
    "dev": "dotenv -- concurrently 'turbo run dev' 'cd frontend && npm run dev'",
    "predev": "npm run generate",
    "generate": "dotenv -- turbo run generate"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "turbo": "latest"
  },
  "name": "shelterapp",
  "packageManager": "npm@8.19.4",
  "workspaces": [
    "wp-plugin/",
    "backend/",
    "wp-plugin/blocks/*"
  ],
  "dependencies": {
    "dotenv-cli": "^7.3.0"
  }
}
